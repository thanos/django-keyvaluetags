============
Installation
============

At the command line::

    $ easy_install django-keyvaluetags

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-keyvaluetags
    $ pip install django-keyvaluetags
